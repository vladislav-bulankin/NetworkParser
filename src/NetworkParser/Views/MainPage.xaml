<Page
    x:Class="NetworkParser.UI.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NetworkParser.UI.Views"
    xmlns:controls="using:NetworkParser.Views.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <!-- TOP BAR -->
        <CommandBar Grid.Row="0">

            <AppBarButton Label="Start"
                   Command="{Binding StartCaptureCommand}" />

            <AppBarButton Label="Stop"
                   Command="{Binding StopCaptureCommand}" />

            <AppBarButton Label="Interfaces"
                   Command="{Binding OpenInterfacesCommand}" />

            <CommandBar.Content>
                <StackPanel Orientation="Horizontal" Spacing="8">

                    <TextBox Width="300"
              PlaceholderText="Filter"
              Text="{Binding FilterText, Mode=TwoWay}" />

                    <Button Content="Apply"
             Command="{Binding ApplyFilterCommand}" />
                    <AppBarButton Label="Apply"
                 Command="{Binding ApplyFilterCommand}" />
                </StackPanel>
            </CommandBar.Content>



        </CommandBar>

        <!-- PACKET LIST -->
        <controls:PacketListView Grid.Row="1"/>

        <!-- DETAILS -->
        <controls:PacketDetailsView Grid.Row="2"/>

        <!-- HEX -->
        <controls:HexViewerView Grid.Row="3"/>
        
    </Grid>
</Page>
